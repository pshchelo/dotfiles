username: "{{ ansible_user_id }}"
userhome: "{{ ansible_user_dir }}"
dotfiles_dir: "{{ userhome }}/dotfiles"
src_dir: "{{ userhome }}/src"
local_bin_dir: "{{ userhome }}/.local/bin"
dev_minimal: false
clean_apt_sources: false
copy_keys: false
